# ========================================
# CVA Root-Level .gitignore
# ========================================

# Python
__pycache__/
*.py[cod]
*.so
.Python
.venv/
venv/
.env

# Common env var files (keep templates like .env.example tracked)
.env.*
!.env.example
!.env.sample

# IDE/Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
logs/
*.log

# Temp files
*.tmp
temp/
tmp/

# Data/artifacts (never commit)
*.db
*.sqlite
*.sqlite3
*.zip
*.tar
*.tar.gz

# Analysis artifacts (regenerated)
CVA_RUN_ANALYSIS_*.md

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# OS files
.DS_Store
Thumbs.db

# TypeScript build cache
*.tsbuildinfo

# Uploaded user files (handled per-subdirectory)
# Note: dysruption_cva/temp_uploads/ is ignored in that dir's .gitignore

# Root temp uploads (never commit)
temp_uploads/*
!temp_uploads/.gitkeep

# Generated reports / scans (never commit)
*.sarif
verification_report.*
pytest_results.txt
file_tree.json

# Defense-in-depth: ignore upload/artifact dirs even if nested ignore changes
# Note: ignore contents, but allow keeping a tracked .gitkeep.
dysruption_cva/temp_uploads/*
!dysruption_cva/temp_uploads/.gitkeep
dysruption_cva/run_artifacts/
dysruption_cva/run_history.json

# Runtime state files
run_history.json
