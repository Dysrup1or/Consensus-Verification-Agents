# Dysruption CVA v1.2 - Environment Variables Template
# Copy this file to .env and fill in your API keys
#
# =============================================================================
# RAILWAY DEPLOYMENT NOTES
# =============================================================================
# For Railway: Set ALL required variables in Railway Dashboard → Service → Variables
# DO NOT commit .env with real keys - use Railway's secure variable storage
# Railway auto-provides: PORT (do not set manually)
# =============================================================================

# =============================================================================
# REQUIRED API KEYS (at least these 4 for full functionality)
# =============================================================================

# Anthropic - Claude (Architect Judge + Prompt Synthesizer)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google AI - Gemini (Extraction + User Proxy Judge)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key

# Groq - DeepSeek R1 Distill (Security Judge + Fallback)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key

# OpenAI - GPT-4o-mini (Remediation/Code Fixes)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# OPTIONAL
# =============================================================================

# DeepSeek Direct API (not currently used - Groq wrapper preferred)
DEEPSEEK_API_KEY=your-deepseek-api-key

# =============================================================================
# SECURITY CONFIGURATION (Production / Railway)
# =============================================================================

# Enable production mode (disables /docs, restricts CORS, sanitizes errors)
# RAILWAY: Set to "true" for production deployments
CVA_PRODUCTION=false

# Allowed CORS origins (comma-separated, only used when CVA_PRODUCTION=true)
# RAILWAY: Set to your frontend Railway domain, e.g.:
# CVA_ALLOWED_ORIGINS=https://cva-frontend-production.up.railway.app
CVA_ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com

# Shared auth token for protected endpoints in production.
# Required when CVA_PRODUCTION=true (used by /upload and /run).
# Clients should send: Authorization: Bearer <token> (or X-API-Token).
CVA_API_TOKEN=

# Max upload size in MB
CVA_MAX_UPLOAD_MB=100

# Rate limit: requests per minute per IP
CVA_RATE_LIMIT=30

# Upload root directory (where /upload writes projects).
# RAILWAY: If you mount a volume at /app/temp_uploads, set CVA_UPLOAD_ROOT=/app/temp_uploads
CVA_UPLOAD_ROOT=

# Per-run artifacts root (each run writes to <root>/<run_id>/...)
# Default is ./run_artifacts if not set.
CVA_RUN_ARTIFACTS_ROOT=

# =============================================================================
# RAILWAY-SPECIFIC (Auto-provided by Railway - DO NOT SET)
# =============================================================================
# PORT - Railway assigns this automatically
# RAILWAY_VOLUME_NAME - Set when volume attached
# RAILWAY_VOLUME_MOUNT_PATH - Mount path for volume (/app/temp_uploads)
# =============================================================================

# Sample project settings (if using the Flask sample)
SECRET_KEY=your-flask-secret-key
DATABASE_URL=sqlite:///tasks.db
